When using set setup_timer_trigger.sh and setup_sysfs_trigger.sh, it's best to run them in that order.

Doing it the other way won't cause any issues, but directory names will be backwards and it will get confusing.
